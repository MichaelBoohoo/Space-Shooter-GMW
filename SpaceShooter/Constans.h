#pragma once
#include <SFML/Graphics.hpp>

const float PLAYER_SPEED = 300.f;
const float PLAYER_SIZE = 50.f;
const float BULLET_SPEED = 600.f;
const float ENEMY_BULLET_SPEED = 300.f;
const float POWERUP_SPEED = 120.f;
const float POWERUP_DURATION = 5.f;

// Kolory
const sf::Color COLOR_FAST = sf::Color::Cyan; 
const sf::Color COLOR_DOUBLE = sf::Color::Magenta; 
const sf::Color COLOR_SHIELD = sf::Color::Green;
const sf::Color COLOR_BOSS = sf::Color::White;
const sf::Color COLOR_ENEMY1 = sf::Color::Red;
const sf::Color COLOR_ENEMY2 = sf::Color(180,0,255);
